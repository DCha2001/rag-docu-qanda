[mypy]
ignore_missing_imports = True
# Exclude testdocs and alembic migration scripts (alembic uses dynamic
# module attributes like op/context that mypy cannot resolve statically)
exclude = (?x)(
    ^testdocs/
    | ^alembic/
  )
